import './App.css'
import { BrowserRouter as Router, Routes, Route, Link } from "react-router-dom";

function App() {

  return (
    <div className="App">
      {/* <Router>
          <Routes>
            <Route index element={} />
            <Route path="/*" element={} />
            <Route path="contact" element={} />
          </Routes>
      </Router> */}
      "hello world"
    </div>
  )
}

export default App
